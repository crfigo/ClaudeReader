# Optional: enables the "Google Cloud TTS" narration engine.
# Create an API key in Google Cloud Console with the Cloud Text-to-Speech API
# enabled, then copy this file to .env.local and fill in the value.
# Without this key, ClaudeReader still works fully using the browser's
# built-in speech synthesis voices.
GOOGLE_TTS_API_KEY=
